{
  "name": "@tjoussen/redux-helper",
  "version": "1.0.0",
  "description": "This package includes helper functions to reduce the redux boilerplate for actions, reducer and middleware",
  "main": "src/index.js",
  "files": [
    "src"
  ],
  "scripts": {
    "publish": "npm run publish --public",
    "test": "jest -c .jest-config.json",
    "test:coverage": "jest -c .jest-config.json --coverage",
    "test:coverage:coveralls": "jest -c .jest-config.json --coverage && ./coverage/lcov.info | coveralls"
  },
  "keywords": [
    "redux"
  ],
  "author": "Thomas Joussen <tjoussen91@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "~7.2.3",
    "@babel/core": "~7.3.4",
    "@babel/preset-env": "~7.3.4",
    "babel-jest": "~24.1.0",
    "coveralls": "^3.0.3",
    "jest": "~24.1.0"
  }
}
